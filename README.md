# Advanced Finance Seminar

This repository contains our empirical analysis for the Advanced Finance Seminar at the University of Copenhagen.

## Purpose
We are investigating how the market reacts to the "Disagreement Gap": the divergence between Management Earnings Guidance and Analyst Consensus estimates. Our research examines both the immediate market reaction (CAR 0 to 1) and the short-window Post-Earnings Announcement Drift (PEAD, looking at windows from 2 to 15 days). We utilize standardized Z-scores to evaluate how this informational complexity impacts price discovery, including tests for asymmetry and various cross-sectional robustness checks.

## Project Structure

- **\data/\**: Contains raw and combined datasets. 
  - \combined/all_relevant_data.csv\: The primary dataset containing 1,000 observations, firm characteristics, guidance data, and computed immediate Cumulative Abnormal Returns (CARs).
  - \combined/all_daily_returns.csv\: Extensive panel data of daily stock returns used to compute alternative dependent variables dynamically (e.g., CAR_2_15).
- **\2/\**: The main repository for our final analysis scripts and outputs.
  - \nalysis_v2.py\: The core empirical pipeline. Executes descriptive statistics, main regressions (immediate reaction), quartile tests, and conditional asymmetry splits. 
  - \
obustness_v2.py\: Supplementary controls. Runs cross-sectional cuts (firm size, sector heterogeneity, and announcement timing) and computes the long-window Buy-and-Hold Abnormal Returns (BHAR).
  - \
esults/\: Contains the output artifacts generated by the scripts, including \ll_analysis_results.csv\, \ll_robustness_results.csv\, \quartile_analysis_results.csv\, and \descriptive_statistics.csv\.
- **\.archive/\**: Contains deprecated iterations (\1/\) and previous single-use scripts that are no longer actively run as part of the pipeline.
- **\
equirements.txt\**: Standard Python dependencies used across the repository.

## How to Run
1. Activate the virtual environment:
   `powershell
   .\.venv\Scripts\Activate.ps1
   `
2. Ensure dependencies are up to date (pip install -r requirements.txt).
3. Execute the analysis scripts from the root of the project:
   `powershell
   python v2/analysis_v2.py
   python v2/robustness_v2.py
   `
The tabular results will auto-generate and compile into the 2/results/ directory.
